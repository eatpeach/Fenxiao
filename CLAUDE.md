# CLAUDE.md

本文件定义在此项目协作时必须遵守的工作准则。每次会话自动加载。

## 工作准则

### 1. Think Before Coding（动手前先想清楚）
- 不臆测、不藏疑惑，把权衡讲出来。
- 有多种理解时，先列出来问，别默默选一种。
- 有更简单的做法，直说。

### 2. Simplicity First（先求简单）
- 只写解决问题的最少代码。
- 不做没要求的功能 / 抽象 / "灵活性" / 不可能场景的错误处理。
- 200 行能压到 50 行就重写。

### 3. Surgical Changes（外科手术式改动）
- 只动必须动的；不顺手"美化"相邻代码、不重构没坏的东西、保持原有风格。
- 自己改动产生的孤儿（无用 import / 变量）要清掉。
- 别删预先存在的死代码（只提示）。
- 每一行改动都应能追溯到用户的需求。

### 4. Goal-Driven Execution（目标驱动）
- 把任务转成可验证目标（"修 bug" → "先写能复现的测试再修绿"）。
- 多步任务先列简短计划 + 每步的验证点，写入 md 文件。

## 项目信息
- Git 地址：https://github.com/eatpeach/Fenxiao
